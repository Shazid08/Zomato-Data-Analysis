# Zomato Data Analysis
📊 Web Scraping Stock Prices with Python  This project demonstrates how to extract live stock prices from websites using web scraping and save the results into a CSV file. It uses three powerful Python libraries:  Requests – Fetches webpage content.  BeautifulSoup – Parses HTML and extracts specific data.  Pandas – Organizes scraped data into a structured DataFrame and saves it as a CSV.  🔍 Approach  Import required libraries.  Loop through a list of stock URLs.  Use BeautifulSoup to extract the current stock price from each page based on class identifiers.  Store the results in a Pandas DataFrame.  Export the final dataset to a CSV file.
